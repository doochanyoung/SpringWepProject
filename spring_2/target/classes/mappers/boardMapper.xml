<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.mapper.BoardMapper">
	<insert id="create">
		insert into tbl_board(title, content, regdate, hit, likes, groups, sequence, isReply, userId) values(#{title}, #{content}, now(), 0, 0, #{maxGroup}, 0, false, #{userId})
	</insert>
	<insert id="createReply">
		insert into tbl_board(title, content, regdate, hit, likes, groups, sequence, isReply, userId) values(#{title}, #{content}, now(), 0, 0, #{groups}, #{sequence}, true, #{userId})
	</insert>
	<select id ="maxGroup" resultType="int">
		select max(groups) from tbl_board
	</select>
	<select id="listAll" resultType="com.java.domain.BoardVO">
		<![CDATA[
			select *
			from tbl_board
			where boardId > 0
			order by groups desc, sequence
		]]>
	</select>
	<select id="read" resultType="com.java.domain.BoardVO">
		select *
		from tbl_board
		where boardId = #{boardId}
	</select>
	<delete id="delete">
		delete from tbl_board where boardId = #{boardId}
	</delete>
	<update id="update">
		update tbl_board set title = #{title}, content = #{content}, updatedate = now() where boardId = #{boardId}
	</update>
	<select id="getGroup" resultType="int">
		select groups from tbl_board where boardId = #{boardId}
	</select>
	<select id="maxSequence" resultType="int">
		select max(sequence) from tbl_board where groups = #{groups}
	</select>
	<select id="listPage" resultType="com.java.domain.BoardVO">
		<![CDATA[
			select * from tbl_board where boardId > 0 order by groups desc, sequence limit #{page}, 30
		]]>
	</select>
	<select id="listPageHandler" resultType="com.java.domain.BoardVO">
		<![CDATA[
			select * from tbl_board where boardId > 0 order by groups desc, sequence limit #{pageStart}, #{perPageNum}
		]]>
	</select>
	<select id="countPaging" resultType="int">
		<![CDATA[
			select count(boardId) from tbl_board where boardId > 0
		]]>
	</select>
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 'title'.toString()">
				and title like concat('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'writer'.toString()">
				and userId like concat('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'content'.toString()">
				and content like concat('%', #{keyword}, '%')
			</if>
		</if>
	</sql>
	<select id="listSearch" resultType="com.java.domain.BoardVO">
		<![CDATA[
			select * from tbl_board where boardId > 0 
		]]>
		<include refid="search" />
		<![CDATA[
			order by groups desc, sequence limit #{pageStart}, #{perPageNum}
		]]>
	</select>
	<select id="listSearchCount" resultType="int">
		<![CDATA[
			select count(boardId) from tbl_board where boardId > 0
		]]>
		<include refid="search" />
	</select>
</mapper>
