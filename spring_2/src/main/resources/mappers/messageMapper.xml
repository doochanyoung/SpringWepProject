<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.java.mapper.messageMapper">

    <insert id="create">
        INSERT INTO tbl_message (messageId, messageContent, messageSender, messageReceiver) 
        VALUES (message_seq.NEXTVAL, #{messageContent}, #{messageSender}, #{messageReceiver})
    </insert>
    
    <select id="read" resultType="com.java.domain.MessageVO">
		select *
		from tbl_message
		where messageId
		= #{messageId}
	</select>
	
	<select id="listall" resultType="com.java.domain.MessageVO">
		select *
		from tbl_message
		where receiverId
		= #{receiverId}
	</select>
</mapper>